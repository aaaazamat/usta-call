# Core
Django>=5.1,<6.1
djangorestframework>=3.15
djangorestframework-simplejwt>=5.3
django-cors-headers>=4.4
django-filter>=24.3
django-environ>=0.11
dj-database-url>=2.2

# i18n — model tarjima qilish uchun
django-parler>=2.3
django-parler-rest>=2.2
# .po -> .mo kompilyatsiya uchun (Render image'da gettext yo'q)
babel>=2.14

# Admin UI (zamonaviy Tailwind asosidagi admin tema)
django-unfold>=0.93

# Database
psycopg[binary]>=3.2

# Real-time
channels>=4.1
channels-redis>=4.2
daphne>=4.1

# Async tasks
celery>=5.4
redis>=5.0

# Media / images
Pillow>=10.4

# AI
anthropic>=0.39
openai>=1.50

# Storage (optional)
django-storages>=1.14
boto3>=1.35

# Geo (optional, manzil/koordinatalar uchun)
# django.contrib.gis ishlatiladi (PostGIS)

# Monitoring
sentry-sdk>=2.14

# Utils
python-slugify>=8.0
phonenumbers>=8.13
httpx>=0.27

# Google OAuth (ID token tekshirish)
google-auth>=2.35
requests>=2.32  # google-auth transport uchun
